# The extracted assets — font, background, soul AND audio — are committed.
# DELTARUNE's sound is cleared for use in these projects, so the drone ships
# with the sequence it belongs to rather than being a local-only luxury.
# tools/extract-gonermaker.sh regenerates all of it from a copy of the game.

# The vendored simulators (DEVICE_KNIGHT, DEVICE_MANTLE) are COMMITTED as
# of the merge: the deployed site must be a self-contained tree, and a
# clone that lacks them is exactly how /DEVICE_KNIGHT/ shipped as an empty
# shell once. tools/vendor-*.sh refresh them; INTEGRATION.md § 1 and 2a-2.

.DS_Store
